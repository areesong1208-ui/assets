# 삼성디스플레이 화질 용어 시멘틱 데이터 구축 — 업무 계획서

## 1. 목표 (Goal)

- **최종 목표**: 영어 → 한글로 번역된 IDMS 문서에 대한 **한글 답변(요약·질의응답)의 품질을 향상**시킨다.
- **핵심 수단**: 삼성디스플레이에서 사용하는 **화질(Picture Quality) 관련 용어**를 추출·정제하여, 번역/질의응답 시스템이 참조할 수 있는 **시멘틱 데이터(도메인 용어 지식베이스)**를 구축한다.
- **왜 필요한가**: 일반 기계번역/LLM은 디스플레이 도메인 전문 용어를 부정확하게 번역하거나(예: *mura* → "무라/얼룩/불균일" 혼용), 사내 표준 표기와 어긋나게 처리한다. 표준화된 용어 지식베이스를 근거(grounding)로 주입하면 번역·답변의 **일관성과 정확도**가 올라간다.

## 2. 산출물 (Deliverables)

| # | 산출물 | 형식 | 상태 |
|---|--------|------|------|
| D1 | 업무 계획서 (본 문서) | Markdown | ✅ 초안 |
| D2 | 화질 용어 시멘틱 데이터 | CSV | ✅ 95개 (일반 55 + IDMS 40) |
| D3 | 시멘틱 데이터 스키마 정의 | 본 문서 §4 | ✅ 초안 |
| D4 | 용어 확장/검증본 | CSV | ⬜ IDMS 원문 대기 |
| D5 | 번역 QA 파이프라인(용어 주입) | 스크립트/프롬프트 | ✅ 프로토타입 |
| D6 | 품질 평가 리포트 | Markdown | ⬜ 예정 |

## 3. 추진 단계 (Phases)

### 1단계 — 용어 수집 (Term Collection)
- 출처: 삼성디스플레이 화질 관련 문서, IDMS 문서, 사내 표준 용어집, 공개 디스플레이 표준(IEC/VESA/SID 용어).
- 방법: 문서에서 화질 관련 명사구 추출 → 후보 용어 리스트업.
- **필요 입력**: IDMS 원문 문서(영/한). *(현재 미보유 — §6 확인 필요)*

### 2단계 — 시멘틱 데이터 스키마 설계 (Schema Design)
- 각 용어를 단순 대역쌍이 아니라 **의미 단위(개념)**로 정의: 영문 표준어, 한글 표준어, 동의어/변형표기, 범주, 정의, 번역 주의사항.
- 시멘틱 관계(상위/하위/관련 개념) 확장 여지 확보 → 후속 온톨로지/지식그래프화 가능.

### 3단계 — 시드 데이터 구축 (Seed Dataset)
- 핵심 화질 용어 50여 개를 범주별로 정리 → `data/display-quality-terms.csv`.
- 검증 가능한 근거(정의·주의사항) 포함.

### 4단계 — 검증 및 확장 (Validation & Expansion)
- 도메인 전문가/표준 대조로 표기·정의 검수.
- IDMS 실제 문서에서 신규 용어 반복 추출하여 확장.

### 5단계 — 번역/답변 품질 향상 적용 (Apply to Translation QA)
- 시멘틱 데이터를 **용어 사전(glossary) / 검색증강(RAG) 근거**로 주입.
- 번역·질의응답 프롬프트에 도메인 용어 제약 조건으로 결합.

### 6단계 — 평가 (Evaluation)
- 적용 전/후 한글 답변 품질 비교(용어 정확도, 표기 일관성, 사람 평가).

## 4. 시멘틱 데이터 스키마 (Schema)

`data/display-quality-terms.csv` 컬럼 정의:

| 컬럼 | 설명 |
|------|------|
| `id` | 용어 고유 ID (예: PQ-001) |
| `category` | 범주 (휘도/명암, 색, 계조, 결함·불량, 시간응답, 해상도·구조, 규격·측정) |
| `en_term` | 영문 표준 용어 |
| `ko_term` | 한글 표준 용어 (권장 표기) |
| `ko_synonyms` | 한글 동의어·변형표기 (`;`로 구분) — 번역 일관성 확보용 |
| `abbr` | 약어 (있는 경우) |
| `definition_ko` | 한글 정의 |
| `translation_note` | 번역 시 주의사항 / 흔한 오역 |

## 5. 일정 (개략)

- 1주차: 스키마 확정 + 시드 데이터(D2) 검토
- 2~3주차: IDMS 문서 기반 용어 확장(D4)
- 4주차: 번역 QA 파이프라인 프로토타입(D5)
- 5주차: 평가 리포트(D6)

## 6. 결정 사항 & 다음 단계 (Decisions & Next Steps)

**결정됨 (2026-08-03):**
1. **시멘틱 데이터 형식** → **용어집(CSV) 확장** 방식으로 진행. (온톨로지는 후속 여지로 남김)
2. **IDMS 문서** → **제공 가능**. 원문(영/한) 업로드 시 실제 문서에서 용어 추출·평가 진행.
3. **적용 대상** → **LLM 프롬프트 주입**. `prompts/translation-qa-prompt.md` + `scripts/build_glossary_prompt.py`로 구현.

**원문 확보 (2026-08-04):**
- IDMS (Information Display Measurements Standard, SID/ICDM 2025) 영문 PDF를 구글 드라이브에서 확인. 챕터별(Ch01~Ch21) 보관.
- **병렬 번역본 없음(영문만)** → 평가는 참조 없는(reference-free) 방식으로 진행: 용어 준수율/표기 일관성/적용 전후 비교.

**IDMS 처리 현황:**
- [x] Ch01 Introduction → 약어·측광/측색 용어 15개
- [x] Ch04 Visual Assessment → 시각평가·결함·계조 용어 25개
- [ ] Ch05 Fundamental Measurements, Ch06 Gray & Color Scale
- [ ] Ch08 Uniformity, Ch09 Viewing Angle, Ch10 Temporal, Ch12 Motion Artifacts
- [ ] Ch20 HDR, Ch21 Color for Display Metrology, 그 외 챕터

**다음 단계:**
- [ ] 남은 챕터를 배치로 처리하여 용어집 확장(D4).
- [ ] 사내 표준 용어집이 있으면 제공 → 정본(canonical) 표기로 대조·정정.
- [ ] 확장된 용어집으로 프롬프트 주입 → 적용 전/후 한글 답변 품질 평가(D6).

**여전히 확인 필요:**
- 사내 표준 용어집(정본) 존재 여부.
- 평가 기준(용어 정확도/표기 일관성/사람 평가 중 우선순위).
- 남은 IDMS 챕터 전체를 처리할지, 화질 핵심 챕터만 우선할지.
