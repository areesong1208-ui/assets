# 작업 지침

이 저장소에서 작업할 때 아래 원칙을 항상 지킨다. 사용자가 매번 명시하지 않아도 기본으로 적용한다.

## 하지 말아야 할 것 (요청하지 않았다면)

- **범위를 벗어나지 않기.** "이것만 고쳐줘"라는 요청에는 그 부분만 고친다. 부탁하지 않은 리팩터링, 옆 코드 정리, 구조 변경을 끼워 넣지 않는다.
- **의존성을 임의로 추가하지 않기.** 새 라이브러리·패키지를 함부로 들이지 말고, 이미 저장소에 있는 것을 우선 사용한다. 새 의존성이 꼭 필요하면 먼저 이유를 설명하고 확인받는다.
- **주석·문서를 대량으로 덧붙이지 않기.** 요청하지 않은 주석, README, 문서 파일을 새로 만들거나 부풀리지 않는다. 주변 코드의 주석 밀도에 맞춘다.
- **스타일·포맷을 임의로 바꾸지 않기.** 한 줄을 고치려고 파일 전체를 리포맷하지 않는다. 기존 코드의 네이밍·들여쓰기·관용구를 그대로 따른다.

## 항상 지킬 것

- **정직하게 보고하기.** 테스트나 타입체크가 실패하면 실패한 그대로, 출력과 함께 보고한다. "통과한 척" 요약하지 않는다. 건너뛴 단계가 있으면 건너뛰었다고 말한다.
- **지정된 브랜치에만 푸시하기.** 지정되지 않은 브랜치(특히 `main`/`master`)에는 푸시하지 않는다.

## 작업 흐름과 충돌할 수 있어 분리한 규칙

아래 규칙은 "작업이 끝나면 지정 브랜치에 커밋·푸시하라"는 자동 작업 흐름과 충돌할 수 있어 별도 파일로 분리했다. 필요 없으면 이 임포트 줄만 지운다.

@.claude/rules/confirm-before-irreversible.md
