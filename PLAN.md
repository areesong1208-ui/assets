# 나만의 첫 화면 대시보드

노트북(Windows) 브라우저 첫 화면 + 휴대폰(Android) 홈 화면을, 참고 이미지 같은
**다크 위젯 대시보드** 느낌으로 만드는 프로젝트.

`index.html` 하나로 노트북·폰 모두 동작하고, 데이터는 각 기기 브라우저에 저장돼요.

---

## 들어간 위젯

| 위젯 | 설명 | 자동/수동 |
|---|---|---|
| ⏰ **시계 + 날짜 + 인사말** | 초 단위 시계, 오늘 날짜, 시간대별 인사 | 자동 |
| 🌤️ **날씨** | 현재 기온 + 5일 예보 (Open‑Meteo, 무료·키 없음) | 자동 (도시 변경 가능) |
| 🟦 **진행 중인 작업** | Claude 작업 5개를 진행률 바로 표시 (💬채팅 / ⌨️코드 태그) | **수동 편집** — 클릭해서 제목·진행률 수정 |
| ✅ **할 일 / 오늘 완성** | 체크하면 '오늘 완성'으로 이동, 매일 자동 초기화 | 자동 저장 |
| 🟣 **습관 트래커** | 매일 체크 + 달성률 링, 매일 초기화 | 자동 저장 |
| 🔗 **바로가기** | 자주 쓰는 사이트 링크 | 수동 추가 |

> ⚠️ "진행 중인 작업"은 Claude 세션을 **자동으로 실시간 연동하는 공개 API가 없어요.**
> 그래서 클릭 한 번으로 5초 만에 고치는 **편집형 보드**로 만들었어요.

---

## 노트북(Windows)에서 첫 화면으로 쓰기

가장 쉬운 방법 두 가지:

**A. 브라우저 홈페이지로 지정 (권장)**
1. 아래 "인터넷에 올리기"로 주소(URL)를 하나 만든다.
2. Chrome/Edge → 설정 → "시작 그룹" 또는 "홈 버튼" → 그 주소 입력.
3. 브라우저 켜면 이 대시보드가 첫 화면.

**B. 새 탭마다 띄우기**
- Chrome 확장 프로그램 "Custom New Tab URL"에 주소를 넣으면 새 탭마다 대시보드가 뜸.

---

## 휴대폰(Android)에서 홈 화면 앱으로 쓰기

1. Chrome로 대시보드 주소를 연다.
2. 우측 상단 ⋮ → **"홈 화면에 추가"**.
3. 홈에 앱 아이콘이 생기고, 누르면 주소창 없이 **앱처럼 전체화면**으로 열림 (PWA).

---

## 인터넷에 올리기 (GitHub Pages · 무료)

이 저장소를 그대로 웹페이지로 publish 하면 됩니다.

1. GitHub 저장소 → **Settings → Pages**
2. **Source: Deploy from a branch** 선택
3. Branch를 `main`(또는 이 작업 브랜치)으로, 폴더는 `/ (root)` 선택 → Save
4. 잠시 뒤 `https://areesong1208-ui.github.io/assets/` 주소가 생김
5. 이 주소를 노트북 홈페이지 / 폰 홈 화면에 등록

> 작업 브랜치에서 먼저 확인하고, 괜찮으면 `main`에 합쳐서 공개하면 됩니다.

---

## 커스터마이즈 (원하면 더)

- **배경 이미지**: 지금은 다크 그라데이션. 참고 이미지처럼 픽셀아트 배경을 넣고 싶으면 말해줘요 (이미지 생성해서 넣어드림).
- **색/글꼴**: `index.html` 상단 `:root` 변수만 바꾸면 전체 톤 변경.
- **위젯 추가**: 캘린더, 명언, 집중 타이머(뽀모도로), D‑day 등 원하는 거 말하면 붙여드림.

---

## 파일

- `index.html` — 대시보드 본체 (이거 하나면 동작)
- `app.webmanifest` — 폰 '홈 화면에 추가'용 앱 설정
- `arriverse-icon.png` — 앱 아이콘
